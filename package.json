{
  "license": "MIT",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build:docs": "rm -rf ./docs && cd packages/with-form-documentation && yarn && rm -rf ./node_modules/with-notification-system/node_modules/react && yarn build && mv -r ./build ../../../docs",
    "clean": "rimraf packages/**/dist",
    "lint": "tslint packages/composable-validation/src/**/*.ts packages/with-form/src/**/*.ts",
    "test": "lerna run test"
  },
  "dependencies": {
    "lerna": "^2.2.0",
    "rimraf": "^2.6.2",
    "tslint": "^5.8.0",
    "typescript": "^2.6.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "tslint-react": "3.2.0"
  }
}
